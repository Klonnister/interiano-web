export interface VisibleFilters {
  categories?: boolean,
  trademarks?: boolean,
  price?: boolean,
  order?: boolean,
  sale?: boolean,
  status?: boolean,
}

export interface DropdownOption {
  name: string,
  value: string,
}